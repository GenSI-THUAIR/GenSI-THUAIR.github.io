repos:
  - repo: local
    hooks:
      - id: pub-bib-updater
        name: Update publications
        entry: bash .githooks/pre-commit-builder
        language: system
        stages: [commit]
        files: 'pub.bib'
